#include <stdio.h>

int main() {
    int num;
    printf("Ingrese un número: ");
    scanf("%d", &num);

    if (num > 0 && num % 2 == 0) {
        printf("El número es positivo y par.\n");
    } else {
        printf("El número no es positivo y par.\n");
    }
    return 0;
}
